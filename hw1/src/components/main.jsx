import { Container } from "@material-ui/core";
import { React } from "react";
import { Header } from "./header";

export const Main = () => {
    return (
        <Container>
            <Header />
        </Container>
    );
};